# Core ML dependencies
torch>=2.0.0
torchaudio>=2.0.0

# Audio processing
sounddevice>=0.4.6
soundfile>=0.12.0
numpy>=1.24.0

# Data processing
pandas>=2.0.0
scikit-learn>=1.3.0

# Progress bars
tqdm>=4.65.0

# Note: For .webm/.ogg audio files (COUGHVID dataset), you may need ffmpeg:
#   Windows: Download from https://ffmpeg.org/download.html and add to PATH
#   Mac: brew install ffmpeg
#   Linux: sudo apt install ffmpeg
